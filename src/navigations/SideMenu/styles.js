import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  imageContainer: {
    marginTop: -18,
    justifyContent: 'center',
    alignItems: 'center',
    // borderWidth: 2,
  },
  imageStyle: {
    height: 180,
    width: 180,
  },
});
